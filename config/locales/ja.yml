ja:
  activerecord:
    errors:
      messages:
        record_invalid: "バリデーションに失敗しました"
        restrict_depend_destroy:
          has_one: "%{record}が存在しているので削除できません"
          has_many: "%{record}が存在しているので削除できません"
    models:
      post: 投稿
    attributes:
      post:
      id: ID
      name: 名前
      description: 内容
      created_at: 登録日時
      updated_at: 更新日時